// @flow 
import * as React from 'react';
type Props = {
    
};
export const LinearProgress = (props: Props) => {
    return (
        <div className='linearProgress'>
            Идет загрузка            
        </div>
    );
};